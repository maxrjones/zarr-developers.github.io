{
  "name": "zarr-developers.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "docs:verify": "node scripts/verify-redirects.mjs && node scripts/verify-links.mjs",
    "verify": "npm run docs:build && npm run docs:verify"
  },
  "devDependencies": {
    "js-yaml": "4.1.1",
    "vitepress": "1.6.4"
  },
  "overrides": {
    "esbuild": "0.25.0"
  }
}
